flask>=2.3
# Pin NumPy <2 to avoid ABI/runtime issues with some compiled ML wheels.
numpy>=1.26,<2
pillow>=10.0
opencv-python-headless>=4.8

# Heavy ML deps (installed in this service's separate Python 3.11/3.12 venv)
torch
torchvision

# Optional engines (installed best-effort by run_local_venv.sh)
# - detectron2 (often requires source build on macOS)
# - mmdet (often requires mmcv/openmim)
